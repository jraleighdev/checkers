export enum PieceColor {
    dark = 'black',
    light = 'white'
}

export enum SquareColor {
    dark = 'brown',
    light = 'tan',
    hover = 'green',
    selectedValid = 'orange',
    selectedInvalid = 'red'
}

